import MenuList from "./MenuList";

const Menu = (props) => {
  return (
    <div className="menu-list">
      <h2 className="menu-list__header">Menu</h2>
      <MenuList />
    </div>
  );
};

export default Menu;
